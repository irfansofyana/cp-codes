#include <bits/stdc++.h>
using namespace std;
int main(){
	ios_base::sync_with_stdio(0);
	int n;
	int A[75][75],B[75][75];
	int i,j;
	
	cin>>n>>n;
	for (i=0;i<n;i++)
		{
			for (j=0;j<n;j++)
				cin>>A[i][j];
		}
	cin>>n>>n;
	for (i=0;i<n;i++)
		{
			for (j=0;j<n;j++)
				cin>>B[i][j];
		}
	
}
