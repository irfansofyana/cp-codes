#include <bits/stdc++.h>
using namespace std;

long long n,k,i,j;

int main(){
	ios_base::sync_with_stdio(false);
	cin >> n >> k;
	for (i = 0 ; i < n ; i++) {
		long long x;
		cin >> x;
		
	}
	return 0;
}