#include <bits/stdc++.h>
using namespace std;

string s;
long long n,m,i,j;
long long ans,temp;
long long w[100005],A[100005];


int main(){
	cin.tie(0);
	cin >> s >> n >> m;
	for (i=0;i<n;i++) {
		cin >> A[i] >> w[i];
	}
	sort(A,A+n);
	
}
