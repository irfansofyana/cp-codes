#include <bits/stdc++.h>
using namespace std;

long long tc,n,i,j,sum;
int kes=1;
long long x;

int main() {
	scanf("%lld",&tc);
	while (tc--) {
		scanf("%lld",&n);
		for (i=0;i<n;i++) {
			scanf("%lld",&x);
		}
	}
}
