#include <bits/stdc++.h>
using namespace std;

int n,i,j;
string s;
vector<string> arr;

int main(){
	ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
	cin >> n;
	for (i = 0 ; i < n ; i++) {
		cin >> s;
		arr.push_back(s);
	}
	
	return 0;
}