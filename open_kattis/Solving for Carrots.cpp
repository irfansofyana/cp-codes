#include <bits/stdc++.h>
using namespace std;

int n,p;
int i;

int main() {
    cin.tie(0);
    cin >> n >> p;
    for (i = 0 ; i < n ; i++) {
        string s;
        cin >> s;
    }
    cout << p << endl;
    return 0;
}
