#include <bits/stdc++.h>
#define OPTIMASI cin.tie(0); ios_base::sync_with_stdio(0);
using namespace std;

int t,n,m,i,j;
int a,b,c;
int A[100005];

int main(){
	OPTIMASI
	cin >> t;
	int tc = 0;
	while (t--){
		cin >> n >> a >> b >> c >> k;
		for (i = 0 ; i < n ; i++) cin >> A[i];
		
	}
	return 0;
}